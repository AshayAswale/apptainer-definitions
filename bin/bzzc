#!/bin/bash
dir="$(dirname $(readlink -f ${BASH_SOURCE[0]}))"
img=../containers/argos_buzz.sif
cmd=$(basename "$0")
arg="$@"
# echo running: singularity exec "${dir}/${img}" $cmd $arg
singularity exec "${dir}/${img}" $cmd $arg
